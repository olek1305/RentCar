<x-layout>
    <x-slot:title>{{ __('messages.contact') }}</x-slot:title>

    <section class="container mx-auto p-6 max-w-3xl">
        <h1 class="text-3xl font-bold mb-4">{{ __('messages.contact') }}</h1>
        <p class="text-gray-700">{{ __('messages.contact_info') }}</p>
    </section>
</x-layout>
